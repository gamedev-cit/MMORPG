{
  "dependencies": {
    "pixi.js": "^4.8.9",
    "socket.io-client": "^2.2.0"
  },
  "devDependencies": {
    "@types/pixi.js": "^4.8.1",
    "@types/socket.io-client": "^1.4.32",
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.8.2"
  },
  "scripts": {
    "dev": "parcel ./src/index.html --open --no-hmr",
    "build": "parcel build ./src/index.html",
    "export": "parcel --public-url 'mmorpg' build ./src/index.html",
    "clean": "rm -rf .cache build",
    "fullclean": "rm -rf .cache build dist node_modules"
  },
  "private": true
}
